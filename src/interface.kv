ScreenManager:
    id: screen_manager
    WelcomeScreen:
    LoginScreen:
    RegisterScreen:
    LibraryScreen:
    ReaderScreen:

<WelcomeScreen>:
    name: 'welcome'
    BoxLayout:
        orientation: 'vertical'
        padding: 50
        spacing: 20
        # ... (Фоновый цвет) ...
        Label:
            text: 'BookFlow' # Заголовок
            font_size: 48
            color: 0, 0, 0, 1
            bold: True
        Widget: # Пустой виджет для растяжения
        AuthButton:
            text: 'Войти' # Кнопка 1
            background_color: 0.7, 0.8, 0.7, 1
            on_release: app.root.current = 'login' # Переход (см. SUB-5.2)
        AuthButton:
            text: 'Зарегистрироваться' # Кнопка 2
            background_color: 0.6, 0.7, 0.9, 1
            on_release: app.root.current = 'register' # Переход (см. SUB-5.2)
        Label:
            text: 'App v1.0'
            # ... (стили) ...
